import React, {useState} from 'react';
import {
  StyledContent,
  StyledTop,
  StyledText,
  StyledNavbar,
  StyledArrow,
  StyledTitle,
} from './style';
import {StatusBar} from 'react-native';

export const NavBar = () => {
  return (
    <StyledNavbar>
      <StyledArrow />
      <StyledTitle>签到好礼</StyledTitle>
      <View />
    </StyledNavbar>
  );
};
